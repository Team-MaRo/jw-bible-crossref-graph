# --- Generated output (recreated by running the tool) ---
*.svg
*.csv
*.html
*.db
*.jwpub
# Renders are regenerated; the only committed image is the small README preview.
*.png
!docs/bible_circle_sm.png

# --- Rust ---
/target
# Cargo.lock IS committed (this is a binary/app) — do not ignore it.

CLAUDE.md
